# AI Pipeline Explanations

## AI ID Verification
Image preprocessing -> field detection/segmentation -> OCR extraction -> structured JSON output.

## AI Data Pipeline
Data source -> streaming ingestion -> AI inference (sentiment) -> warehouse (BigQuery) -> analytics.

## RAG Document Assistant
Document upload -> chunking -> embeddings -> vector store -> LLM-based QA.

## Travel AI Recommender
User preferences -> feature engineering -> ranking -> REST API response.
